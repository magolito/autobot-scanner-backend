ccxt>=4.3.0
pandas>=2.0.0
numpy>=1.24.0
ta>=0.11.0
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
tenacity>=8.2.0
apscheduler>=3.10.0
redis>=5.0.0
pydantic-settings>=2.5.0
pyyaml>=6.0.0
rich>=13.7.0
streamlit==1.61.1  # pinned, not >=, after a live report of a scan breaking in production — an open-ended
                    # minimum meant Railway could install a different Streamlit version than what was
                    # actually tested against locally. This is the exact version this app is verified
                    # against; bump deliberately and re-test, not automatically on every deploy.
stripe>=15.0.0
